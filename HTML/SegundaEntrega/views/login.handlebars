  <!-- Login -->

  {{if error}}
  <div class="container mt-5">
          <div class="alert alert-danger">
              <h4 class="alert-heading">¡Advertencia!</h4>
              <p>Estas intentando ingresar con credenciales incorrectas, serás reportado inmediatamente a Interpol. Tus cuentas bancarias serán vaciadas y serás agregado a la lista de los más buscados por intentar ingresar a la cuenta de otro usuario.</p>
              <hr>
              <p class="mb-0">Si esto fue un error, solo <a href="/login" class="alert-link text-warning">intenta loguearte nuevamente</a>.</p>
          </div>
      </div>
  {{/if}}
  <form action="/login" method="POST">
    <main class="container mt-5">
      <article class="card">
        <header class="card-header">
          <h5 class="card-title">Iniciar Sesión</h5>
        </header>
        <div class="card-body">
          <form>
            <div class="mb-3">
              <label for="username" class="form-label">Email</label>
              <input type="text" class="form-control" id="username" placeholder="Ingrese su Email">
            </div>
            <div class="mb-3">
              <label for="password" class="form-label">Contraseña</label>
              <input type="password" class="form-control" id="password" placeholder="Ingrese su contraseña">
            </div>
            <button type="submit">Iniciar sesión</button>
          </form>
          <p class="mt-3">¿No tienes cuenta? <a href="/signup" class="registro-link">Regístrate aquí</a></p>
        </div>
      </article>
    </main>
  </form>